.DS_Store
.cpcache/
.lsp/
.clj-kondo/.cache/
.nbb/
node_modules/
.env
secrets.edn

# Raw upstream responses are ephemeral and may be large.
data/raw/*
!data/raw/.gitkeep

# Normalized data, projections, chart specs/data, maps, and reports are
# intentional generated repository state and are committed by the daily job.
